# Core HTTP

Organiza infraestrutura de comunicacao HTTP.

Coloque aqui configuracoes globais de requisicao, interceptors, adaptadores e helpers que sejam usados por toda a aplicacao.

Servicos de API especificos de uma feature podem ficar em `features/<nome>/data-access`.
